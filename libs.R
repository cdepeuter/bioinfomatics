# source("https://bioconductor.org/biocLite.R"); ## try http:// if https:// URLs are not supported
# biocLite("affy");
# biocLite("limma");
# biocLite("GEOquery");
# biocLite("hopach")

library(affy);
library(limma);
library(GEOquery);
library(plyr);
library(dplyr);
library(cluster);
library(HSAUR);
library(fpc);
library(TDAmapper);
library(shiny);


